// Re-exported from the app-wide catalog (src/lib/catalog.ts) — kept here so
// existing onboarding imports (`../constants`) don't have to change.
export { GENRE_OPTIONS, LANGUAGE_OPTIONS } from '../../lib/catalog'
